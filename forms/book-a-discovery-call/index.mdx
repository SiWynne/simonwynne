---
title: Book a Free Discovery Call
backLabel: "← Back to Home"
backHref: /
intro: >
  Twenty minutes, no charge, no pitch. Tell me what you're working on and I'll
  tell you honestly whether I can help. Pick a slot that suits you.

formHeading: Confirm your slot
submitLabel: Book this slot
successHeading: You're booked
successMessage: >
  I've sent a calendar invite and a short note about what to have to hand. If
  anything changes, use the reschedule link in that email.

# ─────────────────────────────────────────────────────────────────────────────
# Cal.com owns availability. These two values are the whole configuration: the
# page reads live slots from Cal's public API and writes bookings back to it, so
# a slot disappears the moment it is taken and the booking lands in the Google
# Calendar connected to that Cal account.
#
# Both come from the event type's public link. If that link is
# cal.com/simon-wynne/discovery-call then the username is "simon-wynne" and the
# event slug is "discovery-call".
#
# Slot length, buffers and minimum notice are all set in Cal, not here — this
# page renders whatever Cal says is bookable, then narrows it with the windows
# below.
# ─────────────────────────────────────────────────────────────────────────────
calUsername: "simon-wynne-kemtll"
calEventSlug: "20-minute-discovery-call"

# Which of Cal's slots to actually offer. Cal's schedule can be wider; the page
# only surfaces slots that fall fully inside one of these windows, on weekdays,
# for the rest of this week and next.
#
# Times are wall-clock in `timeZone`, so 10:00 stays 10:00 across the GMT/BST
# switch. Quote them — an unquoted 10:00 is misread as a number in YAML.
availability:
  timeZone: Europe/London
  weekdaysOnly: true
  windows:
    - start: "10:00"
      end: "11:00"
    - start: "15:00"
      end: "16:00"

fallbackEmail: hello@simonwynne.com
---
